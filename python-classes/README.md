# Python - Classes and Objects

This project introduces Object-Oriented Programming in Python: classes,
objects and instances, public/private/protected attributes, the `self`
keyword, the `__init__` method, and using `@property` to build getters
and setters that validate data before it's stored.
