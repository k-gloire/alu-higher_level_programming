# Python - More Classes and Objects

This project deepens the OOP concepts from the previous project: the
__str__ and __repr__ special methods, __del__ for cleanup on instance
deletion, class attributes vs instance attributes, static methods, and
class methods used as alternative constructors (e.g. Rectangle.square()).
