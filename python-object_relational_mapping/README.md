# Python - Object-relational Mapping

This project introduces how Python interacts with MySQL databases using MySQLdb and SQLAlchemy.

The first part uses MySQLdb to connect to MySQL databases and execute SQL queries.

The second part uses SQLAlchemy, an Object-Relational Mapper (ORM), to interact with database tables through Python objects.
